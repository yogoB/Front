# 원본 폰트

`fonts.css`와 `fonts/*.woff2`는 원본 `yogo_v01.html`의 내장 리소스를 복원한 것이다.
원본의 IBM Plex Sans 및 Pretendard Variable 서체와 유니코드 서브셋을 유지한다.
외부 CDN이나 폰트 서버를 요청하지 않는다. 원래 포함된 React·ReactDOM·DC 런타임은 현재 앱에서 사용하지 않는다.
